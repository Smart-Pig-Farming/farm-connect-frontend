import StubPage from "./Stub";

const JoinUsPage = () => {
  return (
    <StubPage
      title="Join Our Community"
      description="Create your account to connect with fellow farmers and access exclusive content."
      backToLabel="Back to Home"
    />
  );
};

export default JoinUsPage;
