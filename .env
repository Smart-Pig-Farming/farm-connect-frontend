# Frontend environment variables for Vite
# WebSocket server URL (Socket.IO backend)
VITE_WS_URL=http://localhost:5000

# Optional: REST API base URL (some hooks fall back to this)
VITE_API_URL=http://localhost:5000
